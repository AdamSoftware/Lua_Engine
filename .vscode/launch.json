{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run LÖVE2D",
            "type": "love",
            "request": "launch",
            "program": "${workspaceFolder}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}